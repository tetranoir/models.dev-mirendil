# Global cross-Region inference profile — served by Bedrock core (Converse), not Mantle, so no [provider] override.
# Profile ID verified via ListInferenceProfiles (ACTIVE) on 2026-09-22; AWS has not published a GPT-6 Luna model card yet.
# Global CRIS pricing = OpenAI API list rates: https://developers.openai.com/api/docs/models/gpt-6-luna
# Converse effort: additionalModelRequestFields.reasoning.effort = none|low|medium|high|xhigh|max (Bedrock's own validation error lists these).
# Live Converse check 2026-09-22 (eu-west-1, global profile): tool call + tool-result round trip; effort low/xhigh/max accepted.
base_model = "openai/gpt-6-luna"
reasoning_options = [{ type = "effort", values = ["none", "low", "medium", "high", "xhigh", "max"] }]
name = "GPT-6 Luna (Global)"

[cost]
input = 0.10
output = 0.50
cache_read = 0.01
cache_write = 0.125

[[cost.tiers]]
tier = { size = 272_000 }
input = 0.20
output = 0.75
cache_read = 0.02
cache_write = 0.25

[modalities]
input = ["text", "image"]
